/*
 * Squatter Windows XP SP3 x86 placeholder.
 *
 * This is intentionally nonfunctional. It exists to prove the pinned MinGW
 * build path and the no-CRT/no-import PE contract before real Squatter agent
 * code is added.
 */

void squatter_entry(void) {
    for (;;) {
    }
}
